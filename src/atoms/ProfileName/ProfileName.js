import React from "react";
import style from "./ProfileName.module.scss";

const ProfileName = (props) => {
  return (
    <div className={style["a-profile-name"]}>
      <span>Tanya Yadav</span>
    </div>
  );
};

export default ProfileName;
